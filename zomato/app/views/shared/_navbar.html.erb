<nav class="navbar navbar-default">
  <div>
    <% flash.each do |name, msg| %>
      <%= content_tag :div, msg, class: "alert alert-info" %>
    <% end %>  
  </div>
  <div class="container">
    <!-- Brand name to go to home page -->
    <div class="navbar-brand">
      <%= link_to "Food.NET!",root_path %>
    </div>
    <% if user_signed_in? %>
      <ul class="nav navbar-nav navbar-right">
        <li> <%= link_to "Sign out", destroy_user_session_path, method: :delete %>  </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" id="userdwn">Welcome <span class="caret"></span></a>
          <ul class="dropdown-menu" aria-labelledby="userdwn">
            <li><%= link_to "My Orders",user_orders_path %></li>
            <li><%= link_to "My Reviews",user_reviews_path %></li>
          </ul>
        </li>
      </ul>
    <% else %>
      <ul class="nav navbar-nav navbar-right">
        <li> <%= link_to "Sign Up", new_user_registration_path %>  </li>
        <li> <%= link_to "Sign In", new_user_session_path %>  </li>
      </ul>
    <% end %>
  </div><!-- /.container -->
</nav>